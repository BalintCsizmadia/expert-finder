import { CustomerRegistrationDetails } from './customer-registration-details';

describe('CustomerLoginDetails', () => {
  it('should create an instance', () => {
    expect(new CustomerRegistrationDetails()).toBeTruthy();
  });
});
